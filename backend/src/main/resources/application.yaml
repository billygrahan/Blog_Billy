server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://aws-0-us-east-2.pooler.supabase.com:5432/postgres?sslmode=require
    username: postgres.jxzjdghtttuauozsciyu
    password: "CamguruPulador@713971"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    schemas: public
    locations: classpath:db/migration

logging:
  level:
    root: INFO
    org.springframework: INFO
    org.flywaydb: DEBUG
    com.blog.backend: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html